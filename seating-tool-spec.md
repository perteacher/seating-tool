# 교실 자리 배치 프로그램 — 개발 스펙

> Claude Code 작업 지시서. 이 파일 내용대로 만들어 주세요.

## 0. 작업 방식 (먼저 읽기)

- **caveman 모드 + cavecrew 위임**으로 작업. 컨텍스트 아끼면서 진행.
- `frontend-design` 스킬 참고해서 촌스럽지 않게, 하지만 화려한 것보다 **깔끔하고 기능이 눈에 바로 보이는** 방향.
- 작업은 아래 **마일스톤 M0~M6 순서**로. 각 마일스톤 끝날 때마다 동작하는 상태로 커밋 단위 정리.
- 완성되면 GitHub Pages 배포 가능한 상태여야 함.

## 1. 무엇을 만드나

중학교 교사가 **교실 자리 배치를 마우스로 짜고, 랜덤으로 섞고, 저장하고, 인쇄**하는 도구.
블로그(perteach.tistory.com) 공유 + 실제 수업용 겸용.

## 2. 절대 조건 (제약)

- **단일 HTML 파일 1개.** 빌드 도구·npm·프레임워크 없음. 라이브러리 필요하면 CDN `<script>`로만.
- **학생 이름(개인정보)은 브라우저 밖으로 한 글자도 안 나감.** 저장은 전부 `localStorage`. 서버·외부 API 호출 없음. (배포 전 PII grep 통과해야 함 — 하드코딩된 이름 없어야 함)
- **회원가입·로그인 없음.** 웹 주소만 열면 바로 사용.
- **한국어 UI, 존댓말.** 교사 대상.
- PC 우선, 모바일은 최소한 명단 확인·인쇄는 되게 (드래그는 터치도 지원하되 PC 경험 우선).

## 3. 데이터 모델 (구현 가이드)

```
students: [
  { id, name, gender: '남'|'여'|null,
    locked: false,        // 셔플해도 이 자리 유지
    needFront: false,     // 앞자리 배치 필요 (시력 등)
  }
]

classroom: {
  rows,                   // 세로 줄 수
  cols,                   // 가로 자리 수
  paired: true|false,     // 2명씩 짝 묶기 여부(분단 사이 통로)
  frontLabel: '칠판/교탁' // 인쇄 시 방향 표시
}

seatMap: { "r,c": studentId | null }   // 자리 → 학생

constraints: {
  keepApart: [ [studentIdA, studentIdB], ... ]  // 서로 떨어뜨릴 짝
}

savedLayouts: [ { name, savedAt, snapshot } ]   // localStorage에 여러 개
```

## 4. 마일스톤

### M0 — 뼈대 + 명단 입력
- 단일 HTML 골격, 상단 툴바 / 좌측 명단 패널 / 우측 자리 격자 레이아웃.
- 명단 입력: **텍스트 붙여넣기**(한 줄에 한 명)로 한 번에 등록 + 개별 추가/삭제.
- 교실 설정: 행·열 수, 짝(2명씩) 여부 설정. 설정하면 빈 격자 렌더링.
- "자동 채우기" 버튼 → 명단을 앞에서부터 자리에 순서대로 배치.

### M1 — 드래그앤드롭
- 학생 카드(이름표)를 **마우스로 끌어 자리 간 이동**.
- 이미 학생이 있는 자리에 놓으면 **두 학생 자리 교체(swap)**.
- 빈자리에 놓으면 이동. 명단 패널로 다시 끌어다 놓으면 자리에서 빼기.
- **터치 드래그도 지원**(모바일 대비).

### M2 — 랜덤 배치 + 자리 고정
- "랜덤 배치" 버튼: 배치된 학생들을 셔플.
- 학생 카드에 **자물쇠 토글(고정)**. 고정된 자리는 셔플해도 그대로.
- 명단에 있지만 자리 없는 학생은 셔플 시 빈자리에 채워짐.

### M3 — 저장 / 불러오기
- 현재 배치를 **이름 붙여 저장**(예: "1학기 3반 6월"). localStorage에 여러 개 보관.
- 저장된 배치 목록 → 불러오기 / 삭제.
- 브라우저 껐다 켜도 마지막 상태 유지(자동 저장).

### M4 — 인쇄
- "인쇄" 버튼 → **인쇄 전용 클린 레이아웃**.
- **칠판/교탁 방향을 위쪽에 크게 표시**(교사가 학생을 마주 보는 방향인지, 학생 시점인지 선택 토글 — 자리표는 방향 헷갈리면 못 씀).
- A4 가로 기준으로 격자가 한 장에 깔끔하게. 툴바·패널 등 UI는 인쇄에서 숨김(`@media print`).
- 상단에 배치 이름/날짜 표시.

### M5 — 제약 조건 (고급, 선택 배치)
- **앞자리 필요 학생**(needFront): 랜덤 배치 시 앞쪽 줄 우선 배정.
- **서로 떨어뜨릴 짝**(keepApart): 지정한 두 학생이 옆·앞뒤로 붙지 않게 셔플. (완벽 보장 어려우면 최선 배치 + 못 지킨 경우 경고 표시)
- 성별 표시가 있으면 인쇄/화면에서 색 구분(옵션, 기본 꺼짐).
- 제약은 **켜고 끌 수 있게**. 기본은 단순 랜덤.

### M6 — 마감
- 반응형 정리, 빈 상태 안내 문구("명단을 붙여넣고 교실 크기를 설정하세요" 등).
- **개인정보 안내 문구**: "입력한 이름은 이 브라우저에만 저장되며 외부로 전송되지 않습니다."
- 파비콘, 페이지 제목(`<title>`), 간단한 사용법 접이식 안내.
- 배포 전 하드코딩된 실명 없는지 확인.

## 5. UX 디테일 (놓치기 쉬운 것)

- 명단 붙여넣기 시 빈 줄·공백 자동 정리.
- 자리 수보다 학생이 많으면 경고, 적으면 남는 자리 빈칸 유지.
- "전체 지우기"는 되돌리기 어려우니 **확인 한 번**.
- 랜덤 배치는 눌렀을 때 살짝 애니메이션 있으면 좋음(과하지 않게).
- 자물쇠 고정 자리는 시각적으로 명확히(테두리·아이콘).

## 6. 배포

- `perteacher.github.io/seating-tool/` (혹은 원하는 이름) GitHub Pages.
- 완성 후 스크린샷 찍을 수 있게 샘플 데이터 넣기 편한 "예시 명단 채우기" 버튼 하나(익명 이름 "학생1~학생30") 있으면 블로그 글 쓸 때 편함.
